#Changes to DB structure
python manage.py makemigrations <filename> {models.py file here}
python manage.py migrate

#Get to the database
python manage.py shell
from music.model import Album,Song

#Two ways of inserting rows to databse
1) a = Album(artist="",album_title="")
2) a = Album()
   a.artist=""
   a.album_title=""

After this we give a.save()

#To display all the tuples
Album.objects.all()
Album.objects.filter(id=1)
#Find strings
Album.objects.filter(artist__startswith='Link')

#Create Related objects set Table1 has the primary key and Table2 is related to
#it via the FK.
variable1 = Table1.objects.get(pk=1 or any id_no)
1)Add the song in the normal way as shown above
2) Take variable1 then perform
variable1.song_set.create(song_title='Sample title',file_type='mp3')



